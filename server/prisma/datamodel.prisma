type User {
  id: ID! @unique
  email: String! @unique
  username: String! @unique
  password: String!
  name: String!
}

## Workaround for no case insensitive search in prisma yet.
type UserIndex {
  id: ID! @unique
  email: String! @unique
  username: String! @unique
  name: String!
}
