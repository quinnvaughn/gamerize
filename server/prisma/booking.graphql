type Booking {
  id: ID! @unique
  numSlots: Int!
  numPlayers: Int!
  players: [User!]!
  total: Float!
  timeslot: GamingTimeSlot!
  invites: [BookingInvite!]!
    @relation(name: "BookingInvites", onDelete: CASCADE)
}
